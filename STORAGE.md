# データ保存方針(STORAGE.md)

- 文書区分:運用文書(T-003で策定)
- 前提:PROJECT_CHARTER.md 原則3(原データ不変)・原則5(機微情報保護)、DATA_MODEL.md(MediaAsset/DerivedAsset)、SECURITY.md

---

## 1. 原則

1. **大容量データ(動画・音声・モデル・DB)はGitリポジトリへ入れない。** 保存場所は `.env` で指定する(`.env.example` 参照)。
2. **原データと派生データを物理的に分離する。** 原データ領域は読み取り専用扱いとし、解析が書き込むのは派生領域のみ。
3. パスはDBに「設定基準の相対パス」で保存し、`.env` のルート設定と組み合わせて解決する(可搬性の確保)。
4. 正確な座標を含むファイルはリポジトリ・データ領域のどちらにも置かない(T-303実装まで。D-12)。

## 2. 標準ディレクトリ構成

`BIO_OBSERVER_DATA_ROOT`(リポジトリ外)配下の既定構成:

```
<DATA_ROOT>/
  originals/            # 原データ(元動画・元音声)。取込後は変更・削除しない
    <project_id>/<site_id>/<station_id>/<survey_session_id>/   # 不透明IDのみで構成
  derived/              # 派生データ(DerivedAsset)。すべて再生成可能性を管理
    proxies/            # 低解像度プロキシ(UI確認用。D-8)
    audio/              # 抽出WAV・音声クリップ
    spectrograms/       # スペクトログラム
    clips/              # 候補動画
    thumbnails/         # サムネイル・軌跡画像
  models/               # モデル本体(BirdNET等)。バージョンごとにサブディレクトリ
  db/                   # SQLiteデータベース
  logs/                 # 解析・アプリケーションログ(正確な座標・地点名を書かない)
```

- 各サブディレクトリは `.env` の個別変数で上書き可能(外付けHDDへ originals だけ置く等)。
- `derived/` 配下の実ファイルは必ず DerivedAsset レコード(系譜・ハッシュ・生成条件)とセットで作成する(D-9)。
- **ディレクトリ名・ファイル名にはDB上の不透明ID(連番・UUID等)のみを使い、地点名・希少種名等の表示名を使用しない**(SECURITY.md:パスからの場所・種の特定を防ぐ。表示名との対応はDBが持つ)。

## 3. バックアップ(D-16)

- 原データ(originals/)とDB(db/)は、ローカル+外部1系統の2重保管を標準とする。
- 原データはSHA-256ハッシュ(MediaAsset保持)で改変・欠損を検知する。
- `derived/` は再生成可能なため必須バックアップ対象としない(ディスク逼迫時の削除対象。D-18)。

## 4. リポジトリ内に置いてよいもの

- ソースコード、設計文書、設定テンプレート(`.env.example`)
- 位置情報を除去・匿名化した小容量のテストフィクスチャ(合成音声等)のみ。実地の録画・録音データは置かない
