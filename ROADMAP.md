# ロードマップ・MVP要件・非機能要件(ROADMAP.md)

- 文書区分:Fable設計文書
- 版:1.0
- 作成日:2026-08-09

---

## 1. 初期MVP要件(必須)

| # | 要件 | 補足 |
|---|---|---|
| 1 | ローカル動画の登録 | ファイル取込・ハッシュ計算・MediaAsset作成 |
| 2 | 調査日時・地点・機材・画角情報の登録 | Project/Site/Station/SurveySession |
| 3 | 元動画のメタデータ取得 | FFprobe。撮影開始日時の確定フロー含む |
| 4 | 音声全体の抽出 | 無劣化または適切形式。全時間対象 |
| 5 | BirdNETによる全時間解析+音声イベント検出 | 映像に鳥が映っているかに関係なく実行。種分類と独立のSEDで未知・低信頼度鳴声も拾う(D-7)。ライブラリはT-003で比較決定(D-13) |
| 6 | 鳴声クリップとスペクトログラム生成 | 前後マージンつき。DerivedAssetとして管理(D-9) |
| 7 | 映像の動体候補抽出 | 差分・背景差分・Optical Flow・追跡。原解像度で解析(タイル分割・複数解像度を選択可能。D-8)。プロキシはUI確認用 |
| 8 | 手前の植物等を除外するマスク設定 | Station単位で保存・再利用 |
| 9 | 候補前後の動画生成 | 前後マージンはパラメータ |
| 10 | サムネイル生成 | 軌跡画像含む |
| 11 | 映像・音声の共通タイムライン | 関連候補の提示(自動確定なし) |
| 12 | 人による確認・修正 | 判定区分は SURVEY_METHOD.md に厳密準拠。履歴保持 |
| 13 | CSV出力 | AI候補と人の判定を区別。位置丸め適用 |
| 14 | 解析履歴の保存 | AnalysisRun(モデル・版・コミットID・パラメータ等)。完了後は凍結(D-10) |
| 15 | 処理失敗時の再開 | JobStep/RunEventによるステップ状態の永続化(D-10) |

## 2. MVPで必須にしないもの

- リアルタイム処理
- クラウド上での重い映像解析
- AIによる種の完全自動確定(これは将来も行わない。憲章 原則1)
- 個体識別の完全自動化
- スマートフォン単体での解析
- 大規模な組織向け権限管理(最低限の3役割のみ。SECURITY.md 第4章)

## 3. 非機能要件

| 分類 | 要件 |
|---|---|
| 処理性能 | 4時間動画をローカルPCで一晩(目安:実時間の2倍以内)で解析完了。目標値は実測後に更新(D-17)。原解像度解析(D-8)と両立しない場合はタイル分割・解像度選択パラメータで調整 |
| 再開性 | 解析はステップ単位で再開可能。二重実行しても結果が重複登録されない |
| 再現性 | 同一入力・同一パラメータ・同一版で同一結果。全Runの条件を記録 |
| 追記性 | AnalysisRun/Review/AccessLog は上書き・物理削除をしない |
| データ量 | 1調査=数十GBの動画を想定。派生データは原データと分離保管し、再生成可能なものは削除可 |
| 可搬性 | ローカル完結。オフライン環境(現地宿舎等)でも確認作業が可能 |
| 保守性 | SQLite→PostgreSQL移行可能なスキーマ。解析ワーカーはジョブ単位で分離 |
| 機微保護 | SECURITY.md の要件をすべて満たす |
| 検証性 | 野帳照合・ベンチマーク再解析が可能(SURVEY_METHOD.md 第5章) |

## 4. マイルストーン

- **M0 設計確定**(本ドキュメント群のレビュー・承認)
- **M1 音声パイプライン**:登録→音声抽出→音声イベント検出+BirdNET種分類→クリップ/スペクトログラム→確認→CSV(音声のみで実調査に使える状態)
- **M2 映像パイプライン**:動体候補抽出(原解像度・タイル分割)→マスク→候補動画/サムネイル→確認
- **M3 統合**:共通タイムライン・関連候補・検証機能(野帳照合+ReferenceObservationによるベンチマーク評価)・アクセス制御(T-303。完了までは正確座標をDBへ入れない)
- **M4 改善ループ**:誤検出集計・ベンチマーク再解析・閾値/マスク調整運用
- 音声を先行させる理由:BirdNETにより早期に実用価値が出る、映像より技術リスクが低い、全時間解析の原則の検証になる(DECISIONS.md D-3)

## 5. 承認済み追加要件(実装待ち)

- **Google Drive受け箱による自動取込・結果返却(T-110 / Issue #6)**:調査者が指定Driveフォルダへ動画を置くだけで、解析PCのworkerが未処理分を自動取得→MediaAsset登録→解析→結果を `results/<job_id>/` へ返却する。Driveは転送手段であり原データの正はローカル(STORAGE.md)。フォルダID・OAuth情報はリポジトリへコミットしない。着手はT-101完了後、独立Issue・独立ブランチ・独立PRで行う。要件詳細はIssue #6

## 6. 将来拡張(設計で妨げないこと)

- 複数カメラの時刻同期
- 複数地点の一括解析
- 飛翔経路の三次元推定
- 定点間での同一個体候補
- 繁殖行動・ディスプレイ飛翔検出
- 巣材運搬・餌運搬・巣立ちの記録
- 地図表示
- 気象データとの統合
- 時間帯・季節・天候別の出現傾向
- 定点録音機への対応
- Raspberry Pi・Jetson等のエッジ処理
- 自動録画
- 自然共生サイトの継続モニタリング
- 猛禽類以外の鳥類・哺乳類・昆虫類への拡張
- 調査報告書の自動生成
- 館山自然デジタルツインとの連携
- BirdNET Custom Classifier(確認済みデータを教師データ化)
