# プロジェクト憲章(PROJECT_CHARTER.md)

- 文書区分:Fable管理文書(上位ルール)
- 版:1.0
- 作成日:2026-08-09
- 状態:承認待ち(調査責任者の確認により確定)

本文書はプロジェクトの目的・非目的・変えてはいけない原則を定義する。
実装上の都合により本文書の原則を変更してはならない。変更が必要な場合は DECISIONS.md に判断事項として記録し、調査責任者の承認を得ること。

---

## 1. プロジェクト名

猛禽類・鳥類長時間モニタリング基盤(コード名:bio-observer)

## 2. 目的

長時間の定点映像と環境音をAIで解析し、鳥類の飛翔・止まり・出入り・鳴声などの候補を漏れなく抽出することで、調査者が確認すべき時間を大幅に短縮する。

- 4時間以上の定点動画を自動解析する
- 鳥類らしい動きがある時刻を抽出し、検出前後の候補動画を自動生成する
- 映像に鳥が映っているかに関係なく、録音された全時間・全鳥類の音声を解析する
- 映像記録と音声記録を、撮影開始日時を基準とした共通タイムラインに統合する
- 調査者が短時間で候補を確認・修正できるようにする
- 確認結果を蓄積し、場所ごとの検出精度を継続的に改善する
- 猛禽類の確認状況、飛翔経路、行動、時間帯、季節変化をデータ化する
- 将来的に自然共生サイト等の継続モニタリングへ展開する

## 3. 対象種の範囲

特定のハチクマだけを対象とした専用ツールではない。

- 映像解析:猛禽類全体へ拡張可能な基盤とする。対象例:ハチクマ、ノスリ、サシバ、オオタカ、ツミ、ハイタカ、クマタカ、ハヤブサ、チョウゲンボウ、ミサゴ、トビ、フクロウ類。猛禽類以外の鳥類も「鳥類候補」として抽出する。
- 音声解析:猛禽類に限定しない。録音された全時間・全鳥類を解析対象とする。
- 将来拡張:猛禽類以外の鳥類・哺乳類・昆虫類への拡張を妨げない構造とする(ROADMAP.md 参照)。

## 4. 非目的(スコープ外)

- AIによる種判定の自動確定(AIは常に「候補」を出すのみ)
- 繁殖確認・営巣確認の自動判定
- リアルタイム警報・侵入検知等の監視用途
- 個体識別の完全自動化
- 一般公開向けの観察記録共有サービス
- 初期MVPにおける、リアルタイム処理・クラウド上の重い映像解析・スマートフォン単体解析・大規模な組織向け権限管理

## 5. 変えてはいけない原則

### 原則1:調査支援であり、自動確定ではない

AI判定と調査者による確認結果をデータ構造・画面・出力のすべてで明確に分離する。

AIの結果は次の表現に限定する:**鳥類候補/猛禽類候補/種候補/行動候補/信頼度/判定根拠**。

AIの種候補だけを根拠に、種確認や繁殖確認として確定してはならない。確定は必ず人(調査者)の判定として記録する。

### 原則2:初期段階ではPrecisionよりRecallを優先する

誤検出が多少増えても、鳥類・猛禽類の見逃しを減らす。風で動く葉、雲、虫、航空機等による誤検出は、人による確認と教師データの蓄積によって徐々に減らす。閾値の初期値は「見逃し最小化」側に設定する。

### 原則3:原データを保存する

元動画・元音声は変更せず保存する。解析によって生成された動画・音声・画像・スペクトログラム・判定結果は、すべて原データから生成された派生データとして系譜(どの原データ・どの解析から生成されたか)を管理する。

### 原則4:再現可能性を確保する

解析ごとに以下を記録する:使用モデル/モデルバージョン/ソースコードのコミットID/解析パラメータ/信頼度閾値/除外領域/解析日時/実行環境/処理時間/エラー/再解析元となった過去解析。

**再解析時に過去の結果を上書きしてはならない。** 解析実行(AnalysisRun)は追記のみとする。

### 原則5:希少種情報を保護する

正確な調査地点、営巣地、止まり場、飛翔経路等は機微情報として扱う。

- 正確な位置情報は権限を持つ調査者のみ閲覧可能
- 外部出力時は位置を丸める
- 公開資料では営巣地を特定できない粒度にする
- 元動画の背景から場所が特定される可能性も考慮する
- 閲覧・出力・変更履歴を記録する

詳細は SECURITY.md を参照。

## 6. 主要ユースケース

### UC-1:定点動画の一括解析(中核ユースケース)
調査者が現地で撮影した4時間超の定点動画を登録し、調査日時・地点・機材・画角を入力する。システムは映像の動体候補抽出と音声全体のBirdNET解析を実行し、候補一覧・候補動画・鳴声クリップ・スペクトログラムを生成する。

### UC-2:候補の確認・修正
調査者は共通タイムライン上で映像候補・音声候補を確認し、種判定・行動判定・誤検出判定を入力する。AI候補と人の判定は別項目として保存される。

### UC-3:誤検出の抑制学習
確認済みの誤検出(葉・虫・雲・航空機等)を原因別に蓄積し、除外領域の調整・閾値調整・将来のモデル改善(BirdNET Custom Classifier等)の教師データとして整理する。

### UC-4:調査結果の出力
確認済みデータをCSV等で出力し、調査報告に利用する。出力時、位置情報は権限と用途に応じて丸められる。

### UC-5:検証(現地記録との照合)
現地調査者が野帳に記録した確認時刻とシステム検出結果を照合し、検出率・見逃し率を評価する(SURVEY_METHOD.md 第5章)。

### UC-6:継続モニタリング(将来)
同一地点での季節・年をまたぐ調査結果を蓄積し、出現傾向・行動・繁殖関連行動の変化を把握する。

## 7. ステークホルダー

| 役割 | 責務 |
|---|---|
| 調査責任者 | 調査計画、判定の最終確認、機微情報権限の管理 |
| 調査者 | 撮影・録音、候補確認、種・行動判定 |
| Fable | 憲章・原則・データモデル・判定区分・意思決定記録の管理 |
| Claude Code | 実装(解析パイプライン、DB、API、確認画面、テスト、ドキュメント) |
| Codex | 独立レビュー・検証・統合(整合性確認、バグ調査、テスト追加、実データ検証、セキュリティレビュー) |

## 8. AI間の役割分担と引き継ぎ

- Fable:本憲章、DATA_MODEL.md、SURVEY_METHOD.md、SECURITY.md、DECISIONS.md、ROADMAP.md を管理する。実装上の都合だけで調査上の原則を変更してはならない。
- Claude Code:実装を担当する。実装判断がFableの原則に影響する場合は、勝手に変更せず DECISIONS.md に判断事項として記録する。
- Codex:独立した実装・検証・統合担当。Fableの原則を上位ルールとして参照する。
- Claude CodeとCodexは、作業開始前に PROJECT_CHARTER.md、AI_HANDOFF.md、TASKS.md を確認し、作業終了時に AI_HANDOFF.md と TASKS.md を更新する。
- 1つのタスクをClaude CodeとCodexが同時編集しない。担当は TASKS.md に記録する。

## 9. Git運用

- mainブランチを直接編集しない
- 機能・修正単位でブランチを分ける
- コミットに変更目的を記載する
- PRには検証方法と結果を書く
- 大容量動画・音声・モデル本体をGitへ入れない
- サンプルデータは位置情報を除去または匿名化する
- モデル・原動画・生成物の保存場所は設定ファイルで管理する
- 秘密情報(APIキー、正確な座標を含む設定)をリポジトリへ保存しない

## 10. 関連文書

| 文書 | 内容 |
|---|---|
| ARCHITECTURE.md | システム構成・解析パイプライン |
| DATA_MODEL.md | データモデル・エンティティ定義 |
| SURVEY_METHOD.md | 調査フロー・判定状態・用語定義・検証計画 |
| SECURITY.md | 機微情報保護・アクセス制御 |
| ROADMAP.md | MVP要件・非機能要件・将来拡張 |
| DECISIONS.md | 意思決定記録・判断待ち事項 |
| TASKS.md | 実装タスク分解・担当 |
| AI_HANDOFF.md | AI間の引き継ぎ・現在の状態・未決事項 |
| CHANGELOG.md | 変更履歴 |
