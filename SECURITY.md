# セキュリティ方針(SECURITY.md)

- 文書区分:Fable管理文書(上位ルール)
- 版:1.0
- 作成日:2026-08-09

---

## 1. 保護対象(機微情報)

- 正確な調査地点の位置情報
- 営巣地・止まり場・飛翔経路(希少種の生息を特定しうる一切)
- 希少種の確認記録(種・日時・場所の組)
- 元動画・元音声(背景の地形・建造物・音から場所が特定される可能性があるため、希少種が関わるものは機微として扱う)

## 2. 基本原則

1. **正確な位置情報は権限を持つ調査者のみ閲覧可能**とする
2. **外部出力時は位置を丸める**(既定粒度は未決事項 P-2。決定までの仮定:公開向けはメッシュ・市区町村レベル、報告書向けは調査責任者が用途ごとに指定)
3. **公開資料では営巣地を特定できない粒度**にする(地点名の仮名化を含む)
4. 元動画・派生動画を外部提供する場合、背景からの場所特定リスクを事前に確認する
5. **閲覧・出力・変更履歴を記録する**(AccessLog、追記のみ)

## 3. データ設計上の対応

- Site は「正確位置(機微)」と「丸め位置(通常表示用)」を分離保持する
- Species に希少種フラグを持ち、希少種に紐づく出力は自動的に強い丸め・抑制の対象とする
- Export には適用した丸め粒度・対象範囲・実行者を必ず記録する
- AccessLog は機微情報(正確位置、営巣地関連、希少種記録)への閲覧・出力・変更をすべて記録し、改変不可とする

## 4. MVPでのアクセス制御

MVPは少人数のローカル利用を前提とし、大規模な組織向け権限管理は実装しない。ただし最低限:

- 役割は「調査責任者(機微閲覧・権限付与可)」「調査者(担当プロジェクトの機微閲覧可)」「閲覧者(丸め済みのみ)」の3区分を設ける
- 正確位置の表示・出力は権限チェックを通す実装とし、AccessLog に記録する
- 将来の詳細な権限管理(組織・プロジェクト単位)へ拡張可能な構造にする

## 5. リポジトリ・データ保管の運用

- 秘密情報(APIキー、パスワード、正確な座標)をGitリポジトリへ保存しない。設定は `.env`/設定ファイル(Git管理外)で扱い、テンプレートのみコミットする
- 大容量の動画・音声・モデル本体はGitへ入れない。保存場所は設定ファイルで管理する
- サンプルデータ・テストデータは位置情報を除去または匿名化する(EXIF/メタデータの座標も除去)
- 原データはハッシュ(SHA-256)で改変検知し、バックアップ手順を定める(手順の具体化は未決事項 P-3)
- 外部サービス(BirdNET APIをクラウドで使う場合等)へ正確な座標を送信しない。解析に座標が有効な場合も丸めた座標を用いる

## 6. インシデント対応

- 機微情報の漏えいが疑われる場合、調査責任者へ即時報告し、AccessLog・Exportの記録から範囲を特定する
- 公開済み資料に特定可能情報が含まれていた場合の回収・修正手順を運用開始前に定める

## 7. AI実装者への遵守事項(Claude Code / Codex)

- 位置情報を含むテストコード・フィクスチャを作らない(ダミー座標を使用)
- ログ・エラーメッセージに正確な座標・地点名を出力しない
- 本文書の要件を緩和する実装変更は禁止。必要な場合は DECISIONS.md に記録し調査責任者の承認を得る
