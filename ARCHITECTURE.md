# システム構成(ARCHITECTURE.md)

- 文書区分:Fable設計文書
- 版:1.0
- 作成日:2026-08-09
- 前提:PROJECT_CHARTER.md の原則を上位ルールとする

---

## 1. 全体像

映像解析と音声解析は**独立して全時間を処理**し、撮影開始日時を基準とした**共通タイムライン**で統合する。

```
元動画(原データ・不変)
 ├─ 映像パイプライン ──> VisualDetection ──┐
 │                                          ├─> 共通タイムライン ─> 確認UI ─> Review ─> Export(CSV等)
 └─ 音声抽出 ─> 音声パイプライン ─> AudioDetection ─┘
                                (すべて AnalysisRun に紐づく派生データ)
```

- 原データ(MediaAsset)は読み取り専用。派生データ(プロキシ、クリップ、サムネイル、スペクトログラム、検出結果)はすべて AnalysisRun 経由で生成され、系譜を保持する。
- 再解析は新しい AnalysisRun として追記する。過去結果の上書き禁止(憲章 原則4)。

## 2. MVPの実行形態

- **ローカル実行**を基本とする。長時間動画の映像解析・音声解析は調査者のPC(またはローカルワークステーション)上で実行する。クラウド上の重い映像解析はMVPでは行わない。
- 構成要素:
  - **取込・解析ワーカー**(Python):FFmpegによるメタデータ取得・音声抽出・クリップ生成、OpenCVによる動体検出、BirdNETによる音声解析(ライブラリはT-003で比較決定。D-13)。ジョブ単位で実行し、実行中の進捗・状態変化・再開情報は JobStep/RunEvent として永続化する(AnalysisRun本体は完了後凍結。D-10)。失敗時は完了済みステップから再開できる。
  - **データベース**:MVPはローカルDB(SQLite想定。将来PostgreSQLへ移行可能なスキーマ設計とする)。
  - **API/確認UI**:ローカルWebアプリ(ブラウザで候補一覧・タイムライン・確認入力)。
  - **ファイルストア**:原データ置き場と派生データ置き場を分離し、パスは設定ファイルで管理する(Git管理外)。

## 3. 映像解析パイプライン

段階的に処理する。各段階の出力は中間成果物として保存し、途中失敗からの再開を可能にする。

1. 動画情報取得(コーデック、解像度、fps、長さ、録画日時メタデータ)
2. 撮影開始日時の確定(メタデータ+人による補正入力。確定値と根拠を記録)
3. 低解像度プロキシ生成(**UI確認・プレビュー用に限定。映像検出には使用しない**)
4. 除外領域・重点領域の設定(手前の植物・常時動く水面等のマスク。Stationごとに保存し再利用)
5. フレーム間差分
6. 背景差分
7. Optical Flow
8. 飛翔体候補の抽出
9. 複数フレーム間の追跡(トラックの生成)
10. 鳥・虫・葉・雲・航空機等の候補分類
11. 猛禽類らしさの判定(大きさ・軌跡・羽ばたき周期・滑翔/帆翔パターン等)
12. 前後時間を含む候補動画生成(前後マージンは解析パラメータ)
13. サムネイル・軌跡画像生成
14. 人による確認(確認UIへ)

検出ごとの記録候補項目は DATA_MODEL.md の VisualDetection を参照。

5〜7は互いに補完する動き検出手法であり、初期は差分系を主、Optical Flowを補助として併用する。閾値・組み合わせは AnalysisRun のパラメータとして記録する(Recall優先の初期値)。

**検出の解像度方針(D-8)**:映像検出は**原解像度**で行うことを基本とし、遠方の小さな飛翔体の見逃しを防ぐため**タイル分割**処理、および処理時間との両立のための**複数解像度(原解像度/縮小解像度)の選択**をパラメータとして指定可能にする。低解像度プロキシは検出には使わず、UI確認・プレビュー専用とする。選択した解像度・タイル設定は AnalysisRun に記録する。

生成される候補動画・サムネイル・軌跡画像・プロキシはすべて DerivedAsset として系譜・ハッシュ・生成条件を管理する(D-9)。

## 4. 音声解析パイプライン

**動画に鳥が映っているかどうかに関係なく、録音された全時間を解析する。**

1. 元動画から音声を無劣化(可能ならstream copy)または適切な形式(48kHz WAV等)で抽出
2. **音声イベント検出(SED)**:種分類とは独立に、音響イベント(鳴声らしい音)の区間を検出する。BirdNETの種候補が得られない**未知・低信頼度の鳴声**もここで拾い上げ、種候補なしの AudioDetection として記録する(D-7。Recall優先の安全網)
3. 音声全体をBirdNET等で種分類解析(地点座標・日付を解析条件として与える。座標の扱いは SECURITY.md 参照。使用ライブラリは公式 birdnet Python library と BirdNET-Analyzer をT-003で比較決定する。D-13)
4. 鳴声候補の開始・終了時刻を記録(SED由来・種分類由来の両方)
5. 種候補・信頼度を記録
6. 前後時間を含む音声クリップを生成
7. スペクトログラムを生成
8. 不明音・低信頼度候補も一定条件で保存(Recall優先。保存条件はパラメータとして記録)
9. 人による聴取・確認(確認UIへ)
10. 確認済みデータを将来のCustom Classifier用教師データとして整理

SED(手順2)と種分類(手順3)は独立した工程であり、同一時間帯の検出は突合して1つの AudioDetection に統合するか、別検出として保持するかをパラメータで制御する。生成されるWAV・クリップ・スペクトログラムは DerivedAsset として管理する(D-9)。

## 5. 共通タイムライン

- 撮影開始日時(確定値)を基準に、映像検出と音声検出を同一時間軸上に表示する。
- 時間的に近接した映像と音声は「**関連候補**」(DetectionLink)として表示できるが、**同一個体・同一事象として自動確定してはならない**。関連の確定は人の判定として記録する。
- 将来の複数カメラ・複数録音機の時刻同期に備え、機器時刻とタイムライン時刻のオフセットを Station/SurveySession 単位で保持できる構造とする。

## 6. 確認UI

- 候補一覧(サムネイル・信頼度・状態フィルタ)
- タイムライン表示(映像・音声を並列表示、関連候補の提示)
- 候補動画・音声クリップ・スペクトログラムの再生
- 判定入力(判定状態は SURVEY_METHOD.md の定義に厳密に従う。AI候補と人の判定は別フィールド)
- 修正履歴の自動記録

## 7. 出力

- CSV出力(確認済み/AI候補を明確に区別する列構成)
- 出力時の位置丸め(SECURITY.md の出力ポリシーに従う)
- すべての出力を Export として記録(誰が・いつ・何を・どの丸め粒度で)

## 8. 技術選定の初期方針(変更時は DECISIONS.md に記録)

| 領域 | 初期選定 | 理由 |
|---|---|---|
| メディア処理 | FFmpeg | 実績・無劣化抽出・クリップ生成 |
| 映像解析 | Python + OpenCV | 差分・背景差分・Optical Flow・追跡の標準的実装。検出は原解像度・タイル分割対応(D-8) |
| 音声解析 | BirdNET+音声イベント検出(SED) | 全鳥類の鳴声解析の事実上の標準。SEDで未知・低信頼度鳴声を補完(D-7)。**ライブラリは公式 birdnet Python library と BirdNET-Analyzer をT-003で比較決定**(D-13。birdnetlibに固定しない) |
| DB | SQLite(MVP)→ PostgreSQL(将来) | ローカル完結。スキーマはDBMS非依存に保つ |
| UI | ローカルWebアプリ | ブラウザで動画・音声・スペクトログラム確認が容易 |
| ジョブ管理 | JobStep/RunEvent(DB上、追記) | 処理失敗時の再開(MVP要件15)。AnalysisRun本体は完了後凍結(D-10) |

## 9. 将来拡張への備え(設計上の考慮のみ、MVPでは実装しない)

- Station に機材・方向・時刻オフセットを持たせ、複数カメラ同期・定点録音機に対応可能とする
- 検出座標・軌跡を正規化して保存し、三次元推定・地図表示へ拡張可能とする
- 解析ワーカーをジョブ単位で分離し、エッジ(Raspberry Pi/Jetson)・クラウドへの配置換えを可能とする
- 気象データ等の外部時系列を共通タイムラインへ重ねられる構造とする
