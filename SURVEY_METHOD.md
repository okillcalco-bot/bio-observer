# 調査方法・判定区分・検証計画(SURVEY_METHOD.md)

- 文書区分:Fable管理文書(上位ルール)
- 版:1.0
- 作成日:2026-08-09

---

## 1. 調査フロー(標準)

1. **計画**:Project・Site・Stationを登録。除外領域(手前の植物等)の初期マスクをStationに設定
2. **現地調査**:定点カメラ・録音機を設置し、長時間(4時間以上)撮影・録音。調査者は野帳に確認種・時刻を記録(検証用)
3. **登録**:動画ファイルを取り込み、SurveySession(調査日時・地点・機材・画角・天候)を登録。撮影開始日時を確定
4. **自動解析**:映像パイプライン(動体候補抽出)と音声パイプライン(BirdNET全時間解析)を独立実行
5. **候補確認**:共通タイムラインで候補動画・音声クリップ・スペクトログラムを確認し、判定を入力
6. **検証**:野帳記録とシステム検出を照合し、見逃し・誤検出を評価
7. **出力**:確認済みデータをCSV出力(位置丸め適用)
8. **改善**:誤検出原因の集計→マスク・閾値調整→(将来)Custom Classifier学習

## 2. 用語定義

| 用語 | 定義 |
|---|---|
| 鳥類候補 | AIが「鳥類の可能性がある」と提示した検出。確定ではない |
| 猛禽類候補 | AIが「猛禽類の可能性がある」と提示した検出。確定ではない |
| 種候補 | AIが提示した種の候補(複数可、信頼度つき)。確定ではない |
| 行動候補 | AIが提示した行動の候補。確定ではない |
| 信頼度 | AIモデルが出力する確度スコア。人の確からしさ評価ではない |
| 判定根拠 | AI:寄与した特徴。人:判定理由の記述 |
| 検出(Detection) | 解析が抽出した時間区間つきのイベント候補 |
| 確認(Review) | 人が検出に対して行った判定。追記のみ |
| 関連候補 | 時間的に近接した映像・音声検出の組。同一事象の確定は人が行う |
| 原データ | 現地で記録された元動画・元音声。不変 |
| 派生データ | 原データから解析で生成された一切(プロキシ、クリップ、画像、結果)。DerivedAssetとして系譜・ハッシュ・生成条件を管理 |
| 撮影開始日時 | 共通タイムラインの基準時刻。確定根拠とともに記録 |
| 音声イベント検出(SED) | 種分類とは独立に、鳴声らしい音響イベントの区間を検出する工程。未知・低信頼度の鳴声を拾い上げる安全網(D-7) |
| 野帳記録 | 調査者が現地で記録した種・時刻。速報的で時刻精度は粗い。見逃しスクリーニング用 |
| ReferenceObservation | 精査済み評価データ。時刻・種を精査した正解データで、ベンチマーク評価(定量指標)の基準(D-11) |

## 3. 確認状態の区分(混同禁止)

以下の区分を厳密に区別する。UI・DB・出力のすべてで同じ区分を使う。

### 3.1 処理状態(システム側)
| 状態 | 定義 |
|---|---|
| 未解析 | メディアは登録済みだが解析未実行 |
| 解析中 | AnalysisRun実行中 |
| AI候補 | 解析完了し、検出が生成された直後の状態 |

### 3.2 確認状態(人の判定)
| 状態 | 定義 |
|---|---|
| 未確認 | AI候補に対し、まだ誰も確認していない |
| 確認済み | 人が確認し、下位のいずれかの結論に至った(総称) |
| 種確定 | 種まで人が確定した |
| 属・科まで確認 | 種は絞れないが属または科まで人が確定した |
| 猛禽類まで確認 | 猛禽類であることまで人が確定した |
| 鳥類まで確認 | 鳥類であることまで人が確定した |
| 種不明 | 鳥類(または対象分類群)だが、それ以上の同定不能と人が判断した |
| 誤検出 | 鳥類ではない(葉・虫・雲・航空機等)と人が判断した。原因分類を記録 |
| 判定不能 | 材料不足で鳥か否かも判断できないと人が判断した |
| 再確認必要 | 一次確認者が保留し、再確認(別の判定者・追加材料)を求めた |

#### 3.2.1 確認状態と判定内容の許容組合せ(DB制約で強制)

| 確認状態 | 必須 | 禁止 |
|---|---|---|
| 種確定 | 種(species_id)、確定粒度=species | — |
| 属・科まで確認 | 確定粒度=genus/family、分類群名(confirmed_taxon) | 種の指定 |
| 猛禽類まで確認 | 確定粒度=raptor | 種の指定 |
| 鳥類まで確認 | 確定粒度=bird | 種の指定 |
| 種不明 | — | 種・確定粒度の指定 |
| 誤検出 | 原因分類(3.3) | 種・確定粒度の指定 |
| 判定不能/再確認必要 | — | 種・確定粒度の指定 |

上記以外の組合せはDBが拒否する。意図的に許容する組合せを追加する場合は本表を先に改訂する(調査責任者の承認要)。

- 「種確定」等の確定系は、必ず判定者・判定日時・判定根拠とともに記録する。
- AIの種候補・信頼度がいくら高くても、Reviewなしに確認状態は「未確認」のまま変わらない(憲章 原則1)。
- 修正は新しいReview行の追記で行い、修正履歴を保持する。

### 3.3 誤検出の原因分類(初期セット)
風で動く葉・枝/雲/虫/航空機・ドローン/鳥以外の動物/水面反射・光/カメラぶれ/ノイズ・圧縮アーチファクト/その他(自由記述)

音声側:風/雨/流水/車両/昆虫/人声・人工音/機材ノイズ/他種の誤同定/その他

## 4. 判定の運用ルール

- 繁殖関連の判定(巣材運搬・餌運搬・ディスプレイ飛翔等)は、AIは「候補」提示まで。繁殖確認としての確定は調査責任者の確認を要する
- 個体識別は人の判定としてのみ記録する
- 判定者間で判断が分かれた場合は「再確認必要」とし、DECISIONS.md ではなく Review 履歴上で解決する(調査データの問題であり設計判断ではないため)

## 5. 検証計画

単純なAI精度だけでなく、調査実務への効果を評価する。

### 5.1 検証指標
| 分類 | 指標 |
|---|---|
| 検出性能 | 実際の鳥類イベントに対する検出率/猛禽類イベントの見逃し率/種別・距離別・背景別の検出率/音声の種別検出率 |
| 誤検出 | 1時間の映像当たり候補数/誤検出の原因別件数 |
| 実務効果 | 人による確認時間/元映像時間に対する確認時間の削減率 |
| コスト | 解析に要した時間/解析コスト |
| 信頼性 | 判定者間の一致率/モデル更新前後の改善量 |

### 5.2 検証方法

野帳照合(粗いスクリーニング)とベンチマーク評価(定量)は役割を分ける(D-11)。

1. **野帳照合機能(スクリーニング)**:SurveySession の現地野帳記録(種・時刻。速報的・時刻精度は粗い)とシステム検出を照合画面で突き合わせ、「野帳にあるが検出なし(見逃し疑い)」「検出あるが野帳になし(新発見または誤検出)」を洗い出す。定量指標の算出には用いない
2. **ベンチマーク評価(定量)**:精査済み評価データ ReferenceObservation(映像・音声を精査して時刻・種を確定した正解データ)を評価セットとし、検出率・見逃し率等の定量指標はこれに対して算出する。モデル・パラメータ変更のたびに同一セットで再解析して改善量を測る(再解析は新Runなので過去と比較可能)
3. **判定者間一致**:同一候補を複数判定者が独立確認し、一致率を記録する運用を定期的に行う
4. **受け入れ目安(MVP)**:初期は数値目標を置かず、実データの計測から開始する(D-14)。猛禽類イベントの見逃し率を最小化する閾値を選び、その上で1時間当たり候補数と確認時間を記録する
